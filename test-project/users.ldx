${ this.def("user", this.use("user.ldx")) }
${
    this.onload(() => {
        return fetch("https://jsonplaceholder.typicode.com/users")
        .then(e => e.json())
        .then(e => def("users", e))
        .catch(e => console.log(e))
    })
}

<ul>
    ${ (this.users && this.users.map) ? this.users.map(e => this.user(e)) : "" }
</ul>