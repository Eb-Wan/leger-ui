<script name="omcompile">
    this._app._globals = { title: "Index", lang: "fr" };
</script>

<template name="main">
    <div>
        <h1>Leger-UI Test</h1>
        <p>This is a button : <span>${ this.use("button.lgs") }</span></p>
    </div>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>
    <span>${ this.use("button.lgs") }</span>

    <ul>
        ${ this.use("users.lgs") }
    </ul>
</template>